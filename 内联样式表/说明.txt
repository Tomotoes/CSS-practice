内联样式是连接样式和标签的最简便的方法，只需要在标签中包含一个 名为style的属性 及其值即可。</br>
其中style属性和它的值之间用“=”连接。</br>
内联样式回向标签中添加很多属性及内容，因此对于网页设计者很维护，更难阅读。而且它只对局部起作用，</br>
因此必须对所有需要的标签多做设置，这样就失去了CSS在控制页面布局方面的优势。</br>
所以，应尽量减少使用内联样式，而采用其他样式。<p style="color:#FF0000;">